
export type Point = {
  x: number,
  y: number,
  z: number,
  r: number,
  selected?: boolean;
}

export type Margin = {
  top: number,
  left: number,
  bottom: number,
  right: number,
}

export type Box = {
  x0: number,
  x1: number,
  y0: number,
  y1: number,
}
